// Middleware temporarily disabled for debugging
// The auth checks are handled in the page components themselves

export { auth as middleware } from "@/auth";

export const config = {
  matcher: [],
};
